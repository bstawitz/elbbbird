<div class="row">
<% @posts.each do |post| %>
	  <div class="span4" style="float: left; margin: 20px; height: 320px;">
		<%= link_to image_tag(post.image.url(:resize), :class => "img-polaroid"), post.image.url %><br>
		<%= post.content %>
		<%= post.user_id %>
		</div>
<% end %>
</div>